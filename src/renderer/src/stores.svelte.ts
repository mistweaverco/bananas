export const activeView = $state({ value: 'join' })

export const navigationEnabled = $state({ value: true })

export const isHosting = $state({ value: false })

export const isWatching = $state({ value: false })

export const hostUrl = $state({ value: '' })

export const participantUrl = $state({ value: '' })
